{
  "mcpServers": {
    "postgres": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://parchmark_user:parchmark_password@localhost:5432/parchmark_db"
      ]
    }
  }
}
