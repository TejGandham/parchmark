# Frontend Production Environment Configuration
# Copy this file to .env.production on your production server
# WARNING: .env.production should NEVER be committed to version control

# API URL (proxied by Nginx, uses relative path)
VITE_API_URL=/api

# Token expiration warning (seconds before expiration to trigger logout)
# Default: 60 seconds
VITE_TOKEN_WARNING_SECONDS=60
