{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://keel.framework/schemas/keel.schema.json",
  "title": "KEEL project config",
  "description": "Project-root configuration consumed by the resolver. v1 carries the security-sensitive invariants list that drives the pretriage signal.",
  "type": "object",
  "required": ["schema_version"],
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1
    },
    "security_sensitive_invariants": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^INV-[0-9]{3,}$"
      },
      "uniqueItems": true,
      "description": "Invariant IDs that mark a feature as security-sensitive when the feature's invariants_exercised intersects this set. Default empty — projects opt in. Surfaced into pretriage as the security_sensitive_inv signal."
    }
  }
}
